<Window
    x:Class="Monica.Windows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Monica.Windows"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Custom Title Bar -->
        <Grid x:Name="AppTitleBar" Height="32">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Image Source="/Assets/Logo.png" Width="16" Height="16" Margin="16,0,16,0"/>
            <TextBlock x:Name="AppTitle" Text="Monica" Style="{ThemeResource CaptionTextBlockStyle}" 
                       VerticalAlignment="Center" Grid.Column="1"/>
        </Grid>

        <!-- Login Frame (shown first) -->
        <Frame x:Name="RootFrame" Grid.Row="1"/>
        
        <!-- Main Content (hidden until login) -->
        <Grid x:Name="MainContent" Visibility="Collapsed" Grid.Row="1">
            <NavigationView x:Name="NavView" PaneDisplayMode="Left" 
                            IsBackButtonVisible="Collapsed"
                            SelectionChanged="NavView_SelectionChanged"
                            Margin="0,0,0,0">
                <NavigationView.MenuItems>
                    <NavigationViewItem Icon="Permissions" Content="密码" Tag="Passwords" />
                    <NavigationViewItem Icon="ProtectedDocument" Content="安全笔记" Tag="Notes" />
                    <NavigationViewItem Icon="Clock" Content="两步验证" Tag="TOTP" />
                    <NavigationViewItem Icon="ContactInfo" Content="卡包" Tag="Cards" />
                    <NavigationViewItem Icon="OutlineStar" Content="密码收藏" Tag="Favorites" />
                    <NavigationViewItemSeparator />
                    <!-- Dynamic categories will be added here in code-behind -->
                    <!-- Category Management Header with Add Button -->
                    <NavigationViewItem x:Name="CategoriesHeader" SelectsOnInvoked="False" 
                                        Icon="Add" Content="新建分类" Tag="AddCategory" 
                                        Tapped="CategoriesHeader_Tapped"/>
                </NavigationView.MenuItems>

                <NavigationView.FooterMenuItems>
                    <NavigationViewItem Content="快捷备份" Tag="QuickBackup" SelectsOnInvoked="False" Tapped="QuickBackup_Tapped">
                        <NavigationViewItem.Icon>
                            <FontIcon Glyph="&#xE792;"/>
                        </NavigationViewItem.Icon>
                    </NavigationViewItem>
                    <NavigationViewItem Content="备份与同步" Tag="WebDAV">
                        <NavigationViewItem.Icon>
                            <FontIcon Glyph="&#xE753;"/>
                        </NavigationViewItem.Icon>
                    </NavigationViewItem>
                </NavigationView.FooterMenuItems>
                <Frame x:Name="ContentFrame" />
            </NavigationView>
        </Grid>
    </Grid>
</Window>
